{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <div class="flex items-center justify-center">
        <div class="rounded-lg shadow-lg p-8 max-w-md w-full bg-white">
            <h2 class="text-2xl font-bold text-center text-gray-800 mb-6">Create an Account</h2>
            {{ form(form) }}
            <p class="text-center text-gray-600 mt-4">Already have an account? <a href="{{ path('app_login') }}" class="text-blue-500 font-semibold">Sign In</a></p>
        </div>
    </div>
{% endblock %}

